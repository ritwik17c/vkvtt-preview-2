# Version 64.1 — Integration Recovery

The v64 implementation files were already present in the supplied package, but the production pages were still Cloud v62.0 and were not loading the v64 add-ons.

This release integrates those decisions into the actual production files and adds Google account switching.

## Fixes
- v64 clean Leave Master Editor promoted to `admin-leave-editor.html`.
- v64 Quick Add Leave wired into Admin Dashboard.
- Today's Proxy Allotment (All Teachers) permanently visible on the homepage.
- Google account chooser added to sign-in and signed-in cloud bar.
- Version/cache links updated to v64.1.
- Added `PROJECT_STATE.md` as a durable project/version ledger.
