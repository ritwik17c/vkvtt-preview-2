(()=>{
  'use strict';
  // Manual printable timetable persistence is now handled independently by
  // vkv-exam-printable-matrix-editor.js. Keeping this legacy bridge inactive
  // prevents manual selections from being pushed into Fixed Date / Session
  // controls used by the automatic scheduler.
})();
