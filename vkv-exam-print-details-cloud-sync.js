(()=>{
  'use strict';
  // Retired compatibility shim. Print-detail migration is now performed only by
  // vkv-exam-shared-official-print-adapter.js against the exact saved schedule ID.
  // Keeping this file inert prevents stale cached loaders from performing title-based
  // writes while preserving a valid script response for older browser caches.
})();