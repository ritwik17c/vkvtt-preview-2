/* Preview2 final metric reconciliation: read-only display correction.
   Current teacher strength = active timetable codes minus superseded transfer-source codes and renamed aliases. */
(async()=>{'use strict';const $=s=>document.querySelector(s);try{const A=await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js'),U=await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-auth.js'),F=await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore-lite.js'),app=A.getApps().length?A.getApp():null;if(!app)return;const auth=U.getAuth(app);if(auth.authStateReady)await auth.authStateReady().catch(()=>{});if(!auth.currentUser)return;const s=await F.getDoc(F.doc(F.getFirestore(app),'master','current'));if(!s.exists())return;const raw=s.data()||{},m={...raw,...(raw.data||{})},known=(m.teachers||[]).map(t=>String(t.code||'')).filter(Boolean),used=new Set(),rx=c=>new RegExp('(^|[^A-Za-z0-9])'+c.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')+'(?=$|[^A-Za-z0-9])');for(const r of m.records||[]){if(Array.isArray(r.codes)&&r.codes.length)r.codes.forEach(c=>used.add(String(c)));else{const e=String(r.entry||'');known.forEach(c=>{if(rx(c).test(e))used.add(c)})}}const old=new Set();for(const h of raw.changeHistory||[]){if(h?.type!=='teacher_timetable_transfer_activation')continue;const c=String(h.fromCode||h.oldCode||'').trim();if(c)old.add(c);else{const z=String(h.summary||'').match(/\(([^()]+)\)\s*→/);if(z?.[1])old.add(z[1].trim())}}Object.keys(m.teacherCodeAliases||{}).forEach(c=>old.add(String(c)));old.forEach(c=>used.delete(c));const n=used.size||((m.teachers||[]).filter(t=>t&&t.active!==false&&!old.has(String(t.code||''))).length);const el=$('#mTeachers');if(el)el.textContent=n;const lab=el?.parentElement?.querySelector('.lab');if(lab)lab.textContent='Teachers in Active Timetable'}catch(e){console.warn('Preview2 metric reconciliation',e)}})();