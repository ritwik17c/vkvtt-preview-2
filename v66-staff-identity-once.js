/* Preview 2 only: three additive interface layers. Runs after the unchanged Preview 1 shell. */
(async()=>{'use strict';const $=id=>document.getElementById(id),safe=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function btn(id,text,fn){let b=$(id);if(!b){b=document.createElement('button');b.id=id;b.textContent=text;b.onclick=fn}return b}function title(text){const d=document.createElement('div');d.className='myAreaTitle';d.textContent=text;return d}function grid(id,cls='myGrid'){const d=document.createElement('div');d.id=id;d.className=cls;return d}function showPanel(html){let p=$('vkv3Panel');if(!p){p=document.createElement('div');p.id='vkv3Panel';p.style.cssText='margin:12px 0;padding:14px;background:#fff;border:1px solid #cbdce5;border-radius:14px';document.querySelector('main.wrap')?.appendChild(p)}p.innerHTML=html;p.scrollIntoView({behavior:'smooth',block:'nearest'})}function periodRows(){return [...($('freePeriod')?.options||[])].map(o=>(o.textContent||'').trim()).filter(x=>x&&!/^Select/i.test(x))}
function cleanupOld(){document.getElementById('v666IdentityCss')?.remove();document.getElementById('v666SafeStaffArea')?.remove();document.body.classList.remove('vkv-nonteaching','vkv-teaching');['myAreaTitle','myAreaGrid'].forEach(id=>{const e=$(id);if(e)e.style.removeProperty('display')});document.querySelectorAll('.nav,.opsGrid').forEach(e=>e.style.removeProperty('display'))}
function build(cat,role,M){cleanupOld();const main=document.querySelector('main.wrap'),banner=$('activeScheduleBanner');if(!main||!banner)return;const oldTitle=$('myAreaTitle'),oldGrid=$('myAreaGrid'),nav=document.querySelector('.nav'),opsTitle=document.querySelector('.opsTitle'),ops=document.querySelector('.opsGrid'),home=document.querySelector('.homebar');
const commonTitle=title('My Area · Common to All Staff'),common=grid('vkv3Common');banner.insertAdjacentElement('afterend',commonTitle);commonTitle.insertAdjacentElement('afterend',common);
const notice=btn('vkv3Notice','🔔 Staff Notice',()=>{const now=new Date(),dmy=v=>{const m=String(v||'').match(/^(\d{2})\/(\d{2})\/(\d{4})$/);return m?new Date(+m[3],+m[2]-1,+m[1],23,59,59):null},a=(M.staffNotices||[]).filter(n=>n&&n.active!==false&&(!n.expiry||!dmy(n.expiry)||dmy(n.expiry)>=now)).sort((a,b)=>(b.priority==='important')-(a.priority==='important')||Number(b.updatedAtMs||0)-Number(a.updatedAtMs||0));showPanel('<h2>Staff Notice</h2>'+(a.length?a.map(n=>`<div style="padding:10px;margin:8px 0;border:1px solid #d9e4ea;border-radius:11px"><b>${n.priority==='important'?'📌 ':''}${safe(n.title)}</b><div style="margin-top:5px;white-space:pre-wrap">${safe(n.body)}</div></div>`).join(''):'<p>No current staff notice.</p>'))});common.append(notice);
[$('myStatusBtn'),$('myAttendanceBtn')].filter(Boolean).forEach(e=>common.append(e));if(nav)[...nav.querySelectorAll('button')].forEach(e=>common.append(e));const free=[...document.querySelectorAll('button')].find(b=>/Free Teachers/i.test(b.textContent||''));if(free)common.append(free);common.append(btn('vkv3TodaySummary','🗂 Today’s Leave / Duty Summary',()=>{const e=$('proxyTodayStatusSummary');showPanel('<h2>Today’s Leave / Duty / Assignment Summary</h2>'+(e?e.innerHTML:'<p>Open Today’s Final Proxy Allotment to load the current summary.</p>'))}));if($('publishedProxyBtn')){ $('publishedProxyBtn').textContent='✅ Today’s Final Proxy Allotment';common.append($('publishedProxyBtn'))}common.append(btn('vkv3Periods','🕐 Period Timings',()=>showPanel('<h2>Period Timings</h2>'+periodRows().map(x=>`<div style="padding:8px;margin:6px 0;background:#f7fafb;border-radius:9px">${safe(x)}</div>`).join('')));if($('annualCalendarBtn')){$('annualCalendarBtn').style.display='';common.append($('annualCalendarBtn'))}const where=[...document.querySelectorAll('button')].find(b=>/Where Now/i.test(b.textContent||''));if(where)common.append(where);
const catTitle=title(cat==='teaching'?'My Work · Teaching':'My Work · Non-Teaching'),catGrid=grid('vkv3Category');common.insertAdjacentElement('afterend',catTitle);catTitle.insertAdjacentElement('afterend',catGrid);if(cat==='teaching'){[$('myTimetableBtn'),$('myProxyTodayBtn'),$('myProxyHistoryBtn')].filter(Boolean).forEach(e=>catGrid.append(e));catGrid.append(btn('vkv3Reminder','🔔 Period Reminder',()=>showPanel('<h2>Period Reminder</h2><p>Teaching-staff period reminder and notification controls.</p>')))}else{catGrid.append(btn('vkv3OfficeDuty','🏢 Office Duty Schedule',()=>showPanel('<h2>Office Duty Schedule</h2><div style="padding:10px;margin:8px 0;border:1px solid #d9e4ea;border-radius:11px"><b>Daily Office Sitting Duty</b><br>Time-slot based duty roster.</div><div style="padding:10px;margin:8px 0;border:1px solid #d9e4ea;border-radius:11px"><b>After-School Stay-Back Duty</b><br>Monday–Saturday · 3:00–4:00 p.m.</div>')))}
const proxy=['admin','manager','proxy_manager'].includes(role),leave=['admin','manager','leave_editor'].includes(role),att=['admin','attendance_manager'].includes(role),admin=role==='admin',delegated=proxy||leave||att||admin;if(delegated){const dt=title('My Delegated Responsibilities'),dg=grid('vkv3Delegated','opsGrid');catGrid.insertAdjacentElement('afterend',dt);dt.insertAdjacentElement('afterend',dg);if(proxy&&$('proxyWorkBtn'))dg.append($('proxyWorkBtn'));if(leave&&$('leaveOpsBtn'))dg.append($('leaveOpsBtn'));if(att)dg.append(btn('vkv3AttendanceManager','📍 Attendance Management',()=>location.href='admin-attendance.html?v=66.0'));if(admin&&$('adminUserAccessBtn'))dg.append($('adminUserAccessBtn'))}
[oldTitle,oldGrid,nav,opsTitle,ops].forEach(e=>{if(e&&e.isConnected)e.style.display='none'});if(home)main.insertBefore(home,main.querySelector('.view'));window.__vkvThreeLayer={category:cat,role};}
try{await new Promise(r=>setTimeout(r,1400));const A=await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js'),U=await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-auth.js'),F=await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore-lite.js'),cfg={apiKey:'AIzaSyDheZpyXghd1aQ9_RLhwpacVriG__wNZW4',authDomain:'vkv-nalbari-timetable.firebaseapp.com',projectId:'vkv-nalbari-timetable',storageBucket:'vkv-nalbari-timetable.firebasestorage.app',messagingSenderId:'791432856951',appId:'1:791432856951:web:61324065a54bef30f98d72'},app=A.getApps().length?A.getApp():A.initializeApp(cfg),auth=U.getAuth(app);if(auth.authStateReady)await auth.authStateReady();const u=auth.currentUser;if(!u)return;const db=F.getFirestore(app),[ps,ms]=await Promise.all([F.getDoc(F.doc(db,'authorizedUsers',u.uid)),F.getDoc(F.doc(db,'master','current'))]),p=ps.exists()?ps.data():{},raw=ms.exists()?ms.data():{},M=raw.data||raw||{},role=String(p.role||'teacher').trim().toLowerCase();let cat=window.__vkvStaffCategory;if(cat!=='teaching'&&cat!=='non_teaching'&&cat!=='administrative'){const t=String(p.staffType||p.staffCategory||'').toLowerCase();cat=t==='teaching'?'teaching':'non_teaching'}if(cat==='administrative')cat='non_teaching';build(cat,role,M)}catch(e){console.warn('Preview 2 three-layer layer failed',e)}})();