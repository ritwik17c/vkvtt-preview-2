import './vkv-qb-admin-pending-age.js?v=20260830-1';
import './vkv-qb-admin-coordinator-report.js?v=20260831-pending-bands-2';
import './vkv-qb-admin-verified-contribution.js?v=20260831-principal-context-3';
import{getApps,getApp}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js';
import{getFirestore,doc,getDoc,collection,query,where,getCountFromServer}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';
const wait=ms=>new Promise(r=>setTimeout(r,ms)),norm=s=>String(s||'').trim().toLowerCase(),txt=s=>String(s||'').trim();
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function syncFilters(){const s=document.getElementById('sLSubject'),c=document.getElementById('sLClass'),sourceS=document.getElementById('lSubject'),sourceC=document.getElementById('lClass');if(s&&sourceS){const old=s.value;s.innerHTML=sourceS.innerHTML;if([...s.options].some(o=>o.value===old))s.value=old}if(c&&sourceC){const old=c.value;c.innerHTML=sourceC.innerHTML;if([...c.options].some(o=>o.value===old))c.value=old}}
function canonical(code,aliases){const seen=new Set();code=txt(code);while(code&&aliases?.[code]&&!seen.has(code)){seen.add(code);code=txt(aliases[code])}return code}
function aliasSet(code,aliases){const can=canonical(code,aliases),set=new Set([can,txt(code)].filter(Boolean));let changed=true;while(changed){changed=false;for(const[old,neu]of Object.entries(aliases||{})){if(set.has(canonical(neu,aliases))||set.has(txt(neu))){if(!set.has(txt(old))){set.add(txt(old));changed=true}}}}return[...set].filter(Boolean)}
async function masterTeachers(db){const s=await getDoc(doc(db,'master','current')),raw=s.exists()?s.data()||{}:{},m=raw.data&&typeof raw.data==='object'?{...raw,...raw.data}:raw,aliases=m.teacherCodeAliases||{},map=m.teacherEmailMap||{},byCan=new Map();for(const t of(m.teachers||[])){if(!t||t.active===false||t.nonTeaching)continue;const rawCode=txt(t.code||t.shortCode||t.teacherShortCode||t.timetableCode||''),code=canonical(rawCode,aliases)||rawCode,name=txt(t.name||t.fullName||rawCode||'Teacher'),email=norm(map[rawCode]||map[code]||t.email||t.gmail||t.googleEmail||t.google_email||''),codes=aliasSet(rawCode||code,aliases);const old=byCan.get(code);if(!old)byCan.set(code,{code,name,email,codes});else{old.codes=[...new Set([...old.codes,...codes])];if(!old.email&&email)old.email=email}}return{teachers:[...byCan.values()].sort((a,b)=>a.name.localeCompare(b.name)),aliases}}
function buildQ(db,field,value,statuses,sub,cls){const p=[collection(db,'qbQuestions'),where(field,'==',value)];p.push(Array.isArray(statuses)?where('status','in',statuses):where('status','==',statuses));if(sub)p.push(where('subject','==',sub));if(cls)p.push(where('className','==',cls));return query(...p)}
async function countOne(db,field,value,sub,cls){const[raw,pending]=await Promise.all([getCountFromServer(buildQ(db,field,value,['submitted','approved','returned'],sub,cls)),getCountFromServer(buildQ(db,field,value,'submitted',sub,cls))]);return{raw:raw.data().count,pending:pending.data().count}}
async function countFor(db,t,sub,cls){let raw=0,pending=0,used=[];for(const code of t.codes||[t.code]){try{const c=await countOne(db,'teacherCode',code,sub,cls);raw+=c.raw;pending+=c.pending;if(c.raw)used.push(code)}catch(e){console.warn('Raw submission code count',code,e)}}if(raw)return{raw,pending,via:used.length>1?'code aliases':'teacherCode'};if(t.email)try{const c=await countOne(db,'teacherEmail',t.email,sub,cls);if(c.raw)return{...c,via:'teacherEmail'}}catch(e){console.warn('Raw submission email count',t.email,e)}return{raw:0,pending:0,via:'teacherCode'}}
async function init(){for(let i=0;i<40&&!document.getElementById('app');i++)await wait(100);const appHost=document.getElementById('app');if(!appHost)return;for(let i=0;i<40&&!getApps().length;i++)await wait(100);if(!getApps().length)return;const db=getFirestore(getApp()),tabs=document.querySelector('.tabs');if(!tabs||document.getElementById('submissionLeaderboard'))return;
const btn=document.createElement('button');btn.type='button';btn.dataset.panel='submissionLeaderboard';btn.textContent='📈 Raw Submissions';tabs.insertBefore(btn,tabs.querySelector('[data-panel="questions"]'));
const sec=document.createElement('section');sec.id='submissionLeaderboard';sec.className='card panel';sec.innerHTML=`<h2>📈 Raw Question Submission Leaderboard</h2><div class="tip">Ranks teachers by questions submitted into the academic workflow, before considering whether they were later verified or returned. Drafts are excluded. Teacher-code aliases such as old/new timetable codes are combined. Aggregate counts are used; question documents are not downloaded.</div><div class="grid2" style="margin-top:10px"><div><label>Subject</label><select id="sLSubject"><option value="">All</option></select></div><div><label>Class</label><select id="sLClass"><option value="">All</option></select></div></div><div class="actions"><button id="loadSubmissionLeaderboard" type="button" class="primary">Calculate Raw Leaderboard</button><button id="exportRawLeaderboard" type="button">Export CSV</button></div><div id="submissionLeaderList"><div class="empty">Click “Calculate Raw Leaderboard”. Nothing is counted on page startup.</div></div>`;appHost.appendChild(sec);syncFilters();
btn.addEventListener('click',e=>{e.preventDefault();syncFilters();document.querySelectorAll('.tabs button').forEach(x=>x.classList.toggle('active',x===btn));document.querySelectorAll('.panel').forEach(x=>x.classList.toggle('active',x===sec))});let last=[];
const calc=document.getElementById('loadSubmissionLeaderboard');calc.addEventListener('click',async e=>{e.preventDefault();e.stopPropagation();const out=document.getElementById('submissionLeaderList'),sub=document.getElementById('sLSubject').value,cls=document.getElementById('sLClass').value;calc.disabled=true;const old=calc.textContent;calc.textContent='Calculating…';out.innerHTML='<div class="empty">Reading active teacher identities…</div>';try{const{teachers}=await masterTeachers(db),rows=[];if(!teachers.length)throw Error('No active teachers were found in the Master Timetable.');const batch=5;for(let i=0;i<teachers.length;i+=batch){const slice=teachers.slice(i,i+batch);out.innerHTML=`<div class="empty">Checking teachers ${i+1}–${Math.min(i+slice.length,teachers.length)} of ${teachers.length}…</div>`;const got=await Promise.all(slice.map(async t=>({...t,...await countFor(db,t,sub,cls)})));for(const x of got)if(x.raw)rows.push(x);await wait(0)}rows.sort((a,b)=>b.raw-a.raw||b.pending-a.pending||a.name.localeCompare(b.name));last=rows;const total=rows.reduce((n,x)=>n+x.raw,0),pending=rows.reduce((n,x)=>n+x.pending,0);out.innerHTML=rows.length?`<div class="tip" style="margin:10px 0"><b>${rows.length}</b> contributing teacher(s) · <b>${total}</b> raw submission(s) · <b>${pending}</b> currently awaiting verification.</div>${rows.map((x,i)=>`<div class="leader"><div class="rank">${i===0?'🥇':i===1?'🥈':i===2?'🥉':'#'+(i+1)}</div><div class="grow"><b>${esc(x.name)}</b><div class="small">${esc(x.code)} · Raw submitted: <b>${x.raw}</b> · Awaiting verification: <b>${x.pending}</b>${x.via==='code aliases'?' · old/new teacher codes combined':''}</div></div></div>`).join('')}`:'<div class="empty">No submitted questions were matched to active teachers for this selection.</div>'}catch(err){console.error('Raw leaderboard:',err);out.innerHTML='<div class="empty" style="color:#8b2d2d">Raw leaderboard could not be calculated: '+esc(err.message||err)+'</div>'}finally{calc.disabled=false;calc.textContent=old}});
document.getElementById('exportRawLeaderboard').onclick=()=>{if(!last.length)return alert('Calculate the Raw Submission Leaderboard first.');const rows=[['Rank','Teacher','Code','Raw Submitted','Awaiting Verification'],...last.map((x,i)=>[i+1,x.name,x.code,x.raw,x.pending])],csv=rows.map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\r\n'),blob=new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='VKV_QB_Raw_Submission_Leaderboard.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)};}
init().catch(e=>console.error('Raw submissions init:',e));